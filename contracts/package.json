{
  "name": "swapbridge-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SwapBridge — swap any EVM ERC20 to native XTZ and bridge it to a tz1 (Tezos X). Foundry contracts + tests + deploy.",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "coverage": "forge coverage --report summary",
    "deploy": "tsx deploy-swapbridge.ts"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0"
  }
}
