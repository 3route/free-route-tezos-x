# Copy to `.env.setup` and fill. Extra keys ONLY scripts/setup.ts needs (mint + list).
# Merged on top of .env. No inline comments after '='.

# Seller tz1 address + secret key (must differ from the buyer). Mints + lists the test NFT.
SELLER_TZ1=
SELLER_TZ1_SK=
# Test FA2 collection address (KT1...) to mint the NFT into.
TEST_FA2=
