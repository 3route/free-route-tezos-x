<!-- Claude Code reads CLAUDE.md; the canonical agent guidance lives in AGENTS.md (the cross-tool standard).
     This file just imports it, so there is a single source of truth. (A git symlink would break on Windows
     clones without core.symlinks, hence a plain one-line import instead.) -->

@AGENTS.md
