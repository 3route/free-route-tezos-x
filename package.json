{
  "name": "objkt-usdc-sdk",
  "version": "0.1.0",
  "type": "module",
  "description": "Tezos-driven pure-SDK: buy an objkt NFT (priced in XTZ) paying with an EVM ERC20, as one atomic Tezos op-group (call_evm approve + 3route swap to alias + fulfill_ask), no proxy contract.",
  "scripts": {
    "check": "tsc --noEmit",
    "setup": "tsx scripts/setup.ts",
    "example": "tsx sdk/index.ts"
  },
  "dependencies": {
    "@taquito/signer": "^21.0.5",
    "@taquito/taquito": "^21.0.5",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "@taquito/rpc": "^21.0.5",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
